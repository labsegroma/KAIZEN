<?xml version="1.0" encoding="utf-8"?><!--
Wrap the DrawerLayout with FrameLayout to use the same View type for the same view ID
across the layout configurations
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <androidx.drawerlayout.widget.DrawerLayout
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:openDrawer="start">

        <include
            android:id="@+id/app_bar_main"
            layout="@layout/app_bar_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <com.google.android.material.navigation.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:fitsSystemWindows="true"
            android:background="@color/white"
            app:headerLayout="@layout/nav_header_main"
            app:menu="@menu/navigation_drawer"
            app:itemIconTint="@null"
            app:itemTextColor="@color/nav_item_color_state"
            app:itemBackground="@drawable/nav_item_selected_bg"
            app:itemHorizontalPadding="20dp"
            app:itemIconPadding="16dp"
            app:itemVerticalPadding="6dp"
            app:itemMaxLines="1"
            app:itemTextAppearanceActive="@style/NavActiveText"
            app:itemTextAppearanceInactive="@style/NavInactiveText" />

    </androidx.drawerlayout.widget.DrawerLayout>
</FrameLayout>